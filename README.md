MainActivity extends grantResults[0] == PackageManager.PERMISSION_GRANTED) {
                sendMessage(+989157089343);
            } else {
                Toast.makeText(this, "Permission denied to send SMS", Toast.LENGTH_SHORT).show();
            }
        }
    }

    private void sendMessage() {
        // Add code 
